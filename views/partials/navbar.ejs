<nav class="sticky top-0 z-50 h-22 w-full border-b border-slate-200 bg-white/95 shadow-md backdrop-blur">
    <div class="relative flex h-full w-full items-center justify-between px-8 lg:px-12">

        <a href="/" class="flex shrink-0 items-center" aria-label="Evently homepage">
            <img src="/images/logos/logo-horizontal-transparent.svg" alt="Evently" class="h-12 w-auto">
        </a>

        <div class="absolute left-1/2 flex -translate-x-1/2 items-center gap-3">
            <a href="/"
                class="rounded-xl px-5 py-3 text-[22px] font-semibold text-slate-700 transition duration-200 hover:bg-indigo-50 hover:text-indigo-600">
                Homepage
            </a>

            <% if (user) { %>
                <a href="/dashboard"
                    class="rounded-xl px-5 py-3 text-[22px] font-semibold text-slate-700 transition duration-200 hover:bg-indigo-50 hover:text-indigo-600">
                    Dashboard
                </a>

                <a href="/profile"
                    class="rounded-xl px-5 py-3 text-[22px] font-semibold text-slate-700 transition duration-200 hover:bg-indigo-50 hover:text-indigo-600">
                    Profile
                </a>
            <% } %>
        </div>

        <div class="flex shrink-0 items-center gap-4">
            <% if (user) { %>
                <a href="/auth/log-out"
                    class="rounded-xl border border-red-200 px-6 py-3 text-[22px] font-semibold text-red-600 transition duration-200 hover:bg-red-50">
                    Logout
                </a>
            <% } else { %>
                <a href="/auth/sign-up"
                    class="rounded-xl px-5 py-3 text-[22px] font-semibold text-slate-700 transition duration-200 hover:bg-slate-100">
                    Sign up
                </a>

                <a href="/auth/log-in"
                    class="rounded-xl bg-indigo-600 px-7 py-3 text-[22px] font-semibold text-white shadow-md transition duration-200 hover:bg-indigo-700">
                    Login
                </a>
            <% } %>
        </div>
    </div>
</nav>