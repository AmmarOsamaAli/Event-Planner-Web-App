<nav class="border-b border-gray-200 bg-white shadow-sm">
    <div class="mx-auto flex max-w-7xl items-center justify-between px-6 py-4">

        <div class="flex items-center gap-2">
            <a href="/"
                class="rounded-lg px-4 py-2 text-sm font-semibold text-gray-700 transition hover:bg-indigo-50 hover:text-indigo-600">
                Homepage
            </a>

            <% if (user) { %>
                <a href="/dashboard"
                    class="rounded-lg px-4 py-2 text-sm font-semibold text-gray-700 transition hover:bg-indigo-50 hover:text-indigo-600">
                    Dashboard
                </a>

                <a href="/profile"
                    class="rounded-lg px-4 py-2 text-sm font-semibold text-gray-700 transition hover:bg-indigo-50 hover:text-indigo-600">
                    Profile
                </a>
            <% } %>
        </div>

        <div class="flex items-center gap-3">
            <% if (user) { %>
                <a href="/auth/log-out"
                    class="rounded-lg border border-red-200 px-4 py-2 text-sm font-semibold text-red-600 transition hover:bg-red-50">
                    Logout
                </a>
            <% } else { %>
                    <a href="/auth/sign-up"
                        class="rounded-lg px-4 py-2 text-sm font-semibold text-gray-700 transition hover:bg-gray-100">
                        Sign up
                    </a>

                    <a href="/auth/log-in"
                        class="rounded-lg bg-indigo-600 px-5 py-2 text-sm font-semibold text-white shadow-sm transition hover:bg-indigo-700">
                        Login
                    </a>
            <% } %>
        </div>

    </div>
</nav>